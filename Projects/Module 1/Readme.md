# Module assessment for module 1 - Cleaning and preparing data

### module task
- combine the first name & surname in columns B and C and have each start with a capital letter
- use the name and surname to create a lowercase email that ends with `@pushin.com`
- split column k `Location` into column L `Floor`, column M `Extension` and column N `Wing`
  
  
